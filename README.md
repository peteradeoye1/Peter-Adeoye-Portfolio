# Peter Portfolio

A Node.js portfolio website inspired by modern founder and product-focused personal sites.

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:3000`.

## Scripts

- `npm start`: start the server
- `npm run dev`: start the server with watch mode
